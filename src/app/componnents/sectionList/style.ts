import {styled} from '@/stitches.config'

export const MySectionList = styled('section', {
  display: 'flex',
  marginTop: '1rem',
  padding: '.5rem',
  background: 'rgba(0,0,0, 0.2)',
  width: '100%',
})
